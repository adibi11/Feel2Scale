using OpenAI;
using OpenAI.Chat;
using OpenAI.Models;
using System.Data;
using System.Net.Http;
using System.Text;
using System.Text.Json;
using Feel2Scale.Data; // Assuming ScaleData is in this namespace
namespace OpenAIApi
{
    internal class Program
    {
        static void Main(string[] args)
        {
        //    OpenAIService openAIService = new OpenAIService();
        //    string userMessage = "Sitting in an empty room tied by hands and legs and can only scream";
        //    ScaleData scaleData = openAIService.GetScaleAttributes(userMessage);
        //    if (scaleData != null)
        //    {
        //        Console.WriteLine(scaleData.ToString());
        //    }
        //    else
        //    {
        //        Console.WriteLine("Failed to retrieve scale data.");
        //    }
        }
    }
}
